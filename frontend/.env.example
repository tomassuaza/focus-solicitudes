# URL del backend (en dev se usa proxy de Vite; en prod debe apuntar a Railway)
VITE_API_BASE_URL=http://localhost:8080

# Google OAuth client ID (mismo que el backend, lado cliente)
VITE_GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
